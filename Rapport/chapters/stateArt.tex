\chapter{State of the Art}

\rodolphe{State of the art, pas un peu boring comme titre?}

\section{The Graph Isomorphism Problem}

\begin{figure}[ht]
    \centering
    \resizebox{\textwidth}{!}{%
    \input{figTex/ex_isomorphisme.tex}%
    }
    \caption{Example of isomorphism between two graphs $G_1$ and $G_2$}
  \end{figure}


The Graph Isomorphism (GI) problem is very well known among computer scientists and mathematicians. It has been such a “star” target for algorithm designers that it was even referred to as a disease by researchers~\cite{read1977graph}. GI belongs to the complexity class $\NP$ (cf.~\cref{def:NP}). However, it is still unknown whether a polynomial-time algorithm exists to solve it. In other words, it is not known whether GI belongs to the complexity class $\P$. Moreover, it is not known to be \NP-complete and the question of whether GI belongs to the complexity class $\coNP$ also remains open. This problem thus continues to raise fundamental questions regarding complexity theory.

GI was referred to as an ``open problem'' in the 1970s, a problem for which it was not known whether it was \NP-complete, belonged to $\P$, or neither~\cite{garey2002computers}. Nowadays, its \NP-completeness is considered highly unlikely, as proving GI \NP-complete would imply a collapse of the polynomial-time hierarchy (\PH) to its second level~\cite{goldreich1991proofs, schoning1988graph}—a result that would have major consequences for computational complexity theory. This is especially problematic because it is widely conjectured that all levels of \PH{} are distinct. If \GI{} were \NP-complete, all levels above the second level would collapse to the second level.

Cependant, quelques recherches nous permettent de rapprocher de plus en plus GI vers certaines classes de complexité. Par exemple, Il a été démontré que The graph Isomorphism is in \hyperref[def:SPP]{SPP}~\cite{arvind2006graph}. Ce qui permet de dire que si 2 graphes sont isomorphes, il n'y a qu'une seule réponse possible à ce ce problème. De plus, 


\rodolphe{Rajouter worst Case et average Case}
For three decades, the fastest proven running time for the Graph Isomorphism problem was $e^{\mathcal{O}(\sqrt{n \log n})}$~\cite{babai1983computational}. But in 2016, it was shown that GI can be solved in quasi-polynomial time: $e^{\log(n){}^{\mathcal{O}(1)}}$~\cite{babai2016graph}, which makes one think even more that GI is only \NP{}-intermediate rather than \NP{}-complete, since otherwise every problem in \NP{} would be solvable in quasi-polynomial time.

\rodolphe{Ici aussi je pourrai potentiellement parler plus de l'algorithme de Babai et de comment il fonctionne. Mais je ne sais pas si c'est utile pour le mémoire. T'en penses quoi?}




Despite the extensive research conducted on GI, its exact complexity status remains unresolved. The discovery of new algorithms has significantly improved our understanding of the problem, but a polynomial-time algorithm for the general case is still unknown.


Nevertheless, we already know polynomial-time algorithms for the Graph Isomorphism (GI) problem on several restricted graph classes. These are typically graph families with particular structural constraints, such as bounded Hadwiger number, a forbidden minor (see~\cite{ponomarenko1988isomorphism} in Russian, as cited in~\cite{mckay2014practical}), or an excluded topological subgraph~\cite{grohe2012structural}. Concrete examples include graphs of bounded degree~\cite{luks1982isomorphism}, bounded tree-width~\cite{bodlaender1990polynomial}, and bounded eigenvalue multiplicities~\cite{babai1982isomorphism}, among others. More recent research has also shown that GI can be solved in polynomial time for graphs of clique-width at most three~\cite{das2020polynomial}. Furthermore, the case of graphs with bounded genus was addressed in earlier work~\cite{filotti1980polynomial}; however, the correctness of the proposed polynomial-time algorithm was later challenged~\cite{myrvold2011errors}.


For some of the graph classes mentioned above, even stronger complexity bounds are known. In particular, GI restricted to planar graphs and interval graphs can be solved in $\LOGSPACE$ (cf.~\cref{def: logspace}), representing a significant improvement in terms of memory usage~\cite{datta2009planar, kobler2011interval}.

As research on the Graph Isomorphism problem has progressed, new polynomial-time algorithms have been developed for increasingly specific graph classes. Unfortunately, most of these algorithms remain mainly of theoretical interest and are difficult to implement efficiently in practice. Notable exceptions include trees~\cite{aho1974design}, planar graphs~\cite{10.1145/800119.803896}, and interval graphs~\cite{colbourn1981linear}, for which practical algorithms are available.

On the other hand, restricting GI to certain graph classes does not necessarily make the problem easier. Indeed, GI remains as hard as the general problem on several seemingly restricted families, including bipartite graphs, chordal graphs, graphs of bounded degeneracy, and graphs of bounded expansion~\cite{grohe2012structural}.


To solve the Graph Isomorphism (GI) problem, three main types of algorithms can be considered~\cite{grohe2012structural}: graph-theoretic algorithms, group-theoretic algorithms, and combinatorial algorithms.

The first family consists of \emph{graph-theoretic algorithms}, classical algorithms that exploit structural properties of specific graph classes. A representative example is the algorithm of Hopcroft and Wong~\cite{hopcroft1974linear} for planar graphs, which relies on the decomposition of the graph into triconnected (3-connected) components and uses graph-theoretic techniques to solve the isomorphism problem efficiently.

The second family comprises \emph{group-theoretic algorithms}, based on computing generators for the automorphism group of a graph. These methods leverage group-theoretic properties and have dominated GI research since the early 1980s. Indeed, GI and the problem of computing a generating set for a graph's automorphism group can each be reduced to the other in polynomial time~\cite{mathon1979note}: the two problems are polynomial-time equivalent, so that solving either one suffices to solve the other. This is the case of Luks' algorithm~\cite{luks1982isomorphism}, which solves the isomorphism problem on graphs of bounded degree by exploiting the structure of the automorphism group to reduce the problem to a sequence of simpler instances.

\rodolphe{Je pourrai aller plus loin ici et parler d'orbite et tout mais je ne sais pas si c'est utile d'aller aussi en profondeur ici. T'en penses quoi?}

Finally, \emph{combinatorial algorithms} are simple, general-purpose procedures that do not rely on the specific structure of graph classes. All combinatorial algorithms are based on the idea of \emph{colour refinement} (the 1-dimensional version of the Weisfeiler-Leman algorithm~\cite{kiefer2020power}), which aims to compute a \emph{stable colouring} of a graph, as illustrated in \cref{alg:colour-refinement}. Le color refinement algorithm est une des idées les plus simple pour tester l'isomorphisme entre deux graphes. Il a été réutilisé et modifier énormément de fois.  Cependant, la plus vieille version de l'algorithme date de 1965~\cite{morgan1965generation} pour des structures chimiques. 

\begin{algorithm}
\caption{Colour Refinement Algorithm~\cite{grohe2012structural}}\label{alg:colour-refinement}
\begin{algorithmic}[1]
\State\textbf{Initialisation:} Assign the same initial colour to all vertices.
\Repeat{}
    \State\textbf{Refinement Step:}
    \ForAll{pairs of vertices $v, w \in V$}
        \ForAll{colours $c$ in the current colouring}
            \If{the number of neighbours of $v$ of colour $c$ differs from that of $w$}
                \State{Assign different colours to $v$ and $w$}
            \EndIf{}
        \EndFor{}
    \EndFor{}
\Until{(the colouring is stable)}
\end{algorithmic}
\end{algorithm}



To use colour refinement as an isomorphism test, we apply it to the disjoint union of the input graphs $G$ and $G'$. The algorithm \emph{distinguishes} $G$ and $G'$ if there exists a colour $c$ in the stable colouring such that $G$ and $G'$ contain different numbers of vertices of colour $c$. Colour refinement \emph{identifies} a graph $G$ if it distinguishes $G$ from all non-isomorphic graphs; that is, no non-isomorphic graph shares the same stable colouring as $G$. However, if two graphs share the same stable partition, this is not a sufficient condition to prove that they are isomorphic; it is only a necessary one. Therefore, colour refinement does not fully solve the Graph Isomorphism problem, but rather provides a useful indication.

Colour refinement is not only used in the context of Graph Isomorphism; it also plays a role in reducing the cost of solving linear programs~\cite{grohe2014dimension}, as well as in computing graph kernels~\cite{shervashidze2011weisfeiler}.

\input{figTex/colorRefinement.tex}

Regarding the time complexity, for a graph $G$ with $n$ vertices and $m$ edges, a stable colouring can be reached after at most n refinement steps. In fact, it can be computed in time $\mathcal{O}((n + m)\log n)$~\cite{cardon1982partitioning}, and has a lower bound of $\Omega((n + m)\log n)$ for the natural class of iterative partition refinement algorithms~\cite{berkholz2017tight}. This gives a tight bound of $\Theta((n + m)\log n)$ for such algorithms.

Now that the concept of colour refinement is well defined, we can turn to the types of combinatorial algorithms that build upon it. The most direct generalisation is the \textit{Weisfeiler-Leman algorithm} (or $k$-WL)~\cite{kiefer2020power}. Its underlying principle remains largely the same as that of colour refinement, except that instead of comparing individual vertices, the algorithm operates on $k$-tuples of vertices. The central concept it introduces is that of \textit{$i$-neighbours}: for $\vec{v}, \vec{w} \in V^k$ and $i \in \{1, \dots, k\}$, the tuples $\vec{v}$ and $\vec{w}$ are said to be $i$-neighbours if $v_j = w_j$ for all $j \neq i$. 

\begin{algorithm}
\caption{$k$-dimensional Weisfeiler-Leman ($k$-WL) algorithm}
\begin{algorithmic}[1]
\Require{Graph $G = (V, E)$, integer $k \geq 1$}
\Ensure{Stable colouring of $V^k$}

\State\textbf{Initialization:}
\ForAll{tuples $\vec{v}, \vec{w} \in V^k$}
    \If{$\vec{v} \not\cong \vec{w}$} \Comment{No isomorphism between induced subgraphs}
        \State{Assign different initial colours to $\vec{v}$ and $\vec{w}$}
    \EndIf{}
\EndFor{}

\Repeat{}
    \State\textbf{Refinement step:}
    \ForAll{tuples $\vec{v}, \vec{w} \in V^k$}
        \For{$i = 1$ to $k$}
            \State{Compare multisets of colours of $i$-neighbours of $\vec{v}$ and $\vec{w}$}
            \If{they differ}
                \State{Assign different colours to $\vec{v}$ and $\vec{w}$}
            \EndIf{}
        \EndFor{}
    \EndFor{}
\Until{the colouring is stable}

\end{algorithmic}
\end{algorithm}

\rodolphe{Rajouter exemple de $k$-WL sur un graphe simple}


An interesting result is that for all graphs $G$ and $H$, $2$-WL can distinguish $G$ and $H$ if and only if colour refinement can distinguish them. In other words, despite the fact that $2$-WL operates on pairs of vertices and provides a richer representation of the graph structure, it has the same distinguishing power as colour refinement for the graph isomorphism problem~\cite{grohe2012structural}.

However, the situation changes when considering dimensions higher than two. For every $k \geq 3$, $k$-WL is strictly more powerful than $(k-1)$-WL.\@ In particular, while $2$-WL and colour refinement have the same distinguishing power, $3$-WL can distinguish graph pairs that are indistinguishable by $1$-WL and $2$-WL.\@

The reason behind this improvement is that $k$-WL considers tuples of $k$ vertices instead of individual vertices. Consequently, it can capture more global structural relationships between vertices. In contrast, $1$-WL has a more local perspective, as it only refines vertex colours based on the multiset of colours of their neighbours. By increasing $k$, the algorithm is able to analyse interactions between several vertices simultaneously and therefore detect structural differences that remain invisible to lower-dimensional versions.

For example, $3$-WL can solve the graph isomorphism problem for planar graphs~\cite{kiefer2019weisfeiler}. This result illustrates the increasing expressive power of higher-dimensional Weisfeiler-Leman algorithms.

Regarding its complexity, for a graph $G$ with $n$ vertices, the stable colouring of $k$-WL can be reached after at most $n^k$ refinement rounds. The stable colouring can then be computed in time $\mathcal{O}(n^k \log n)$.


Beyond this tuple-based generalisation, En pratique, c'est assez lourd de lancer $2$-WL algorithm sur des graphes. En règle général, dans les current isomorphism package, nous allons utiliser le color refinement comme vu précédemment. Cela reste sufficient pour la majorité des graphes. Si par contre, on remarque que ça ne suffit pas, alors l'algorithme va passer au concept de \textit{l'individualisation}. This isa second and quite different family of approaches relies on \textit{individualisation-refinement}. This technique consists of fixing vertices (individualisation) combined with the refinement of partitions of the vertex set; it was first introduced by Parris and Read~\cite{parris1969coding}, as cited in~\cite{mckay2014practical}. Ça va permettre de casser la symétrie and subsequently color refinement can be potentially
applied again to produce a more refined partition of the vertices.~\cite{grohe2020graph}. Its first implementation appeared in \Naut{}, whose key idea is to construct a \textit{search tree} in which individualisation and refinement are applied recursively to guide the traversal. During the exploration of the search tree, a \textit{node invariant} is computed at each node to guide the search and prune unnecessary branches. Once the search is complete, the information gathered throughout this exploration is used to determine a \textit{canonical labelling} of the graph, that is, a unique representation, or canonical form, of the graph. This canonical labelling lies at the heart of solving the Graph Isomorphism problem (GI), since two graphs are isomorphic if and only if their canonical forms coincide. These notions will be revisited and studied in much greater detail in the next chapter.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{imgs/pré-mémoire.png}
    \caption{Example of GI with canonical labelling from~\cite{mckay2024nug}}\label{fig: canonical labbeling}
\end{figure}


Over time, several other GI solvers based on the individualisation-refinement paradigm have emerged, each building on or extending the ideas underlying \Naut. \textit{Saucy}, introduced in 2004, follows the same principle but was designed to outperform \Naut{} on sparse graphs~\cite{darga2004exploiting}. A few years later, \textit{Bliss} was released; it also computes canonical forms but incorporates additional techniques that improve performance on more complex graphs (\cite{junttila2011conflict}, as cited in~\cite{mckay2014practical}).\textit{Conauto}, introduced in 2009, has shown remarkable efficiency on random graphs and on certain families of hard instances~\cite{lopez2011conauto}. Finally, \textit{Traces} was developed~\cite{piperno2008search} as a tool able to compete with \Naut{} on the Graph Isomorphism problem; today, Traces and \Naut{} are tightly integrated and distributed together~\cite{mckay2014practical}.



\medskip

Finally, an alternative approach to solving GI relies on linear programming. Given the adjacency matrices of two graphs $G$ and $H$, they are isomorphic if and only if there exists a permutation matrix $X$ such that $X^\top A X = B$.

This formulation can be relaxed using LP techniques, notably the Sherali-Adams hierarchy. In particular, level-$k$ Sherali-Adams relaxations offer a powerful framework that has been investigated for graph isomorphism testing~\cite{grohe2012structural}.

\rodolphe{Je parlerai un peu plus de linear programming ici en mettant un peu plus de contexte/exemple ou je laisse comme ça?}



